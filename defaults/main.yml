---
pgsql_listen_port: 5432
pgsql_user: 'developer'
pgsql_user_passwd: 'password'
pgsql_database_name: 'development'
pgsql_replication_user: 'repl_user'
pgsql_replication_passwd: 'password'
pgsql_replication_hosts: "{{ ansible_play_hosts }}"

pacemaker_cluster_name: 'pgsqlcluster'
pacemaker_cluster_hosts: "{{ ansible_play_hosts }}"
pacemaker_hacluster_password: 'password'
pacemaker_cluster_info: # {virtual_ipaddr: '192.168.33.10', check_interface: 'eth1'}
pacemaker_pcs_pgsql_config_file_path: '/var/lib/pacemaker/cib/cib-pgsql.xml'
